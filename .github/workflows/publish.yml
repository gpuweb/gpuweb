name: Build and publish

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2

      - name: Build with bikeshed
        run: |
          git clone --depth 1 https://github.com/tabatkins/bikeshed.git
          pip3 install --user --editable ./bikeshed
          export PATH="$(python3 -m site --user-base)/bin:${PATH}"
          bikeshed update
          mkdir out
          ./compile.sh

      - name: Deploy to GitHub Pages (for push)
        if: ${{ success() && github.ref == 'refs/heads/main'}}
        uses: JamesIves/github-pages-deploy-action@releases/v3
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages
          FOLDER: out
          CLEAN: false

      - name: Upload Spec preview (for PR)
        if: ${{ success() && github.ref != 'refs/heads/main' && github.event.number }}
        uses: actions/upload-artifact@v2
        with:
          name: spec-preview
          path: out/index.html

      - name: Upload WGSL preview (for PR)
        if: ${{ success() && github.ref != 'refs/heads/main' && github.event.number }}
        uses: actions/upload-artifact@v2
        with:
          name: wgsl-preview
          path: out/wgsl.html

      - name: Upload IDL preview (for PR)
        if: ${{ success() && github.ref != 'refs/heads/main' && github.event.number }}
        uses: actions/upload-artifact@v2
        with:
          name: idl-preview
          path: out/webgpu.idl
